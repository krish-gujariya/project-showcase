<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table, td ,th{
            border: 1px solid;
            border-collapse: collapse;
            text-align: center  ;
        }
        .nav ,.box {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .box{
            padding-left: 20px;
        }
        .nav{
            justify-content: space-between;
            background-color: black;
            color: white;
            padding: 7px 20px;
            font-size: 17px;

        }
        .nav a{
            text-decoration: none;
            color: black;
            font-size: 17px;
            border: 1px solid;
            padding: 5px 10px;
            border-radius: 8px;
            background-color: white;
        }
        .nav a:hover{
            background-color: #8b8181;
        }
        .box{
            display: flex;
            align-items: baseline;
        }

        th a{
            text-decoration: none;
            color: black;

        }
        th a:visited{
            color: black;
        }
        th{
            background-color: antiquewhite;
        }
        th,td{
            padding: 7px;
        }
        .hsd:hover{
            background-color: blue;
            color: white;
        }
        h1{
            text-align: center;
        }
        table,.nav{
            margin: auto;
        }
        

    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="nav">
        <div class="box">
            
            <a href="/attendance-list?page=1&values=<%= value %>&Year=<%= year %>"><<</a>
            <a href="/attendance-list?page=<%=--init; %>&values=<%= value %>&Year=<%= year %>" <% if (init==0) { %>
             style = "pointer-events: none;
             cursor: default;"
            <% } %> ><</a>
            <p><%= ++init; %>/<%= Math.ceil(Count/200) %></p>
            <a href="/attendance-list?page=<%= ++init; %>&values=<%= value %>&Year=<%= year %>"<% if (init==251) { %>
                style = "pointer-events: none;
                cursor: default;"
            <% } %>>></a>
            <a href="/attendance-list?page=250&values=<%= value %>&Year=<%= year %>">>></a>
        </div>
        
        <div class="dropdown">
            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= month %> <%= year %>
            </button>
            <ul class="dropdown-menu dropdown-menu-dark">
              <li><a class="dropdown-item hsd active" href="/attendance-list?page=1&values=12&Year=2023">December 2023</a></li>
              <li><a class="dropdown-item hsd active" href="/attendance-list?page=1&values=1&Year=2024">January 2024</a></li>
              <li><a class="dropdown-item hsd active" href="/attendance-list?page=1&values=2&Year=2024">February 2024</a></li>
            </ul>
          </div>
        <div class="total">
            <p>Total number of records are :- <%= Count %></p>
        </div>
    </div>
        <table border="1px">
            <tr>
                <th>Student id</th>
                <th>Student Name</th>
                <th>Present Day</th>
                <th>Attendance Percentage</th>
                <th>Null Column</th>
            </tr>

            <% Data.forEach(element => { %>
             <tr>
                <td><%= element.Student_Id %></td>
                <td><%= element.fname %></td>
                <td><%= element.days %></td>
                <td><%= element.Percentage %></td>
                <td>NULL</td>
             </tr>
            <% }) %>

        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>